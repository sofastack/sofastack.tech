name: deploy to aliyun oss

on: 
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: ls && pwd
#    # 切代码到 runner2244
      - uses: 15669072513/alipay-cloud-devops-codescan@main

