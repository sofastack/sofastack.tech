name: Upload to OSS

on:
  push:
    branches:
      - master  

jobs:
  upload:
    runs-on: ubuntu-latest

    steps:
    - name: Upload to OSS
      run: |
        ossutil cp -r . oss://sofastack-sofastacktech/ 
